<h1>New Feed</h1>

<%= render 'form', feed: @feed %>

<%= form_for @feed do |f| %>
  <p>
    <%= f.label :content,"ツイート" %>
    <%= f.text_field :content %>
  </p>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', feeds_path %>
